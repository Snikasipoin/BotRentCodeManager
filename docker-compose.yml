version: "3.9"

services:
  bot:
    build: .
    container_name: cs2_rent_bot
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    command: python -m bot.main